[project]
name = "pre-warm-poc"
version = "0.1.0"
description = "Pre-warm POC for Path AI - Calendly to AI chat integration"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.27.2",
    "python-dotenv>=1.0.1",
    "pydantic[email]>=2.9.2",
    "pydantic-settings>=2.6.0",
    "openai>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend"]
